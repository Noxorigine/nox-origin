name: NØX ACT 18 - Reputation & Trust

on:
  workflow_dispatch:
  schedule:
    - cron: "37 */6 * * *"

permissions:
  contents: write

concurrency:
  group: nox-act18-reputation-trust
  cancel-in-progress: false

jobs:
  reputation-trust:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build ACT18 Reputation & Trust
        shell: bash
        run: |
          set -euo pipefail

          python3 <<'PY'
          import json
          import os
          import re
          import hashlib
          from datetime import datetime, timezone

          KNOWLEDGE_FILE = "memory/nox_knowledge_core.json"
          OUTPUT_FILE = "memory/nox_reputation_trust.json"

          os.makedirs("memory", exist_ok=True)

          now = datetime.now(timezone.utc).isoformat()

          with open(KNOWLEDGE_FILE, "r", encoding="utf-8") as f:
              knowledge_data = json.load(f)

          entries = knowledge_data.get("knowledge", [])

          # ------------------------------------------------------------
          # NØX identity exclusion
          # ------------------------------------------------------------

          NOX_NAMES = {
              "nox",
              "nox origin",
              "nox_origine",
              "nox-origin",
              "noxorigine",
              "nøx",
              "nøx origin",
              "nøx_origine",
              "agent nox",
              "nox agent",
          }

          NOX_UUIDS = {
              "442104b5-eee0-4f73-b452-08f258a21ea2"
          }

          # ------------------------------------------------------------
          # Technical / non-agent vocabulary
          # ------------------------------------------------------------

          INVALID_NAMES = {
              "unknown",
              "none",
              "null",
              "unclear",
              "unresolved",
              "
